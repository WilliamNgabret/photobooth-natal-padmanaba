// Feature flags and configuration
export const config = {
  // Set to true when you have a verified Resend domain
  ENABLE_EMAIL_SENDING: false,
  
  // Photo expiry time in hours
  PHOTO_EXPIRY_HOURS: 24,
};
